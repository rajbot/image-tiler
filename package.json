{
  "name": "image-tiler",
  "version": "0.1.0",
  "description": "Image tiling application with Rust/WASM backend",
  "main": "index.js",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg && webpack --mode production",
    "dev": "wasm-pack build --target web --out-dir pkg && webpack serve --mode development",
    "wasm-build": "wasm-pack build --target web --out-dir pkg",
    "preview": "npm run build && npx http-server dist -p 8080",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1",
    "html-webpack-plugin": "^5.5.4",
    "copy-webpack-plugin": "^11.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "@babel/preset-env": "^7.23.0",
    "babel-jest": "^29.7.0"
  },
  "dependencies": {}
}